@using ContentModels = Umbraco.Web.PublishedModels
@model Umbraco.Core.Models.Blocks.BlockListItem

@{
    var block = Model.Content as ContentModels.ContentBlockList;
}
<ul>
    @foreach (var item in block.ListItems)
    {
        <li>@item</li>
    }
</ul>